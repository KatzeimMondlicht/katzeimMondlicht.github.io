---
layout: default
---
	{% comment %}
	这应该能显示tag页面才对啊
	{% endcomment %}

<header>
	<h1>Tag: {{ page.title }}</h1>
	{{ content }}
</header>

{% assign tag = site.tags[page.title] %}
{% include series.liquid posts=tag %}
