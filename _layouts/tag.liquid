---
layout: default
---
	{% comment %}
	这应该能显示tag页面才对啊
	哦 这是生成模式 页面的基础
	{% endcomment %}

<h1>Tag: {{ page.title }}</h1>

{% assign tags = site.tags[page.title] %}
{% include series.liquid posts=tags %}

	<ul>
		{% for post in site.posts %}
		{% if post.tags contains page.tag %}
			<li><a href="{{ post.url }}">{{ post.title }}</a></li>
		{% endif %}
		{% endfor %}
	</ul>